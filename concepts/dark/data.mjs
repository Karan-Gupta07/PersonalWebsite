// Bridge: pull the real site content into the static dark mockups so
// every theme shows identical copy and differs on design alone.
// Rendered to plain HTML at build time by build.mjs.
export * from '../../content/site.js';
